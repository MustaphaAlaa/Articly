@* @using Entities.Domain; *@
@model List<Tag>
@{
    ViewData["Title"] = "All Tags";
}

<h1 class="h1 text-center" > Tag  List</h1>
    

<a asp-controller="tags" asp-action="AddTag" class="btn btn-primary m-2">Add Tag</a>

<table class="table table-striped table-bordered m-2    ">
    <thead>
        <tr>
            <td>Tag Name</td>
                <td>  DisplayName  </td>
                <td>Optinos </td>
            </tr>
    </thead>
    <tbody>
        @foreach(var tag in Model)
            {
                <tr>
                    <td>@tag.Name</td>
                    <td>
                    <a asp-action="GetTag" asp-controller="tags" asp-route-id="@tag.TagId" class="  link-primary">@tag.DisplayName</a>
                      </td>
                    <td>
                    <a asp-action="updatetag" asp-controller="tags" asp-route-id="@tag.TagId" class="btn btn-warning">Edit</a>
                </td>
                </tr>
            }
        </tbody>

</table>